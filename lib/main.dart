import 'package:flutter/material.dart';
import 'package:quizzr/quiz_screen.dart';

// This main function (in the main.dart file) is the entry point of every app as it is automatically executed by Dart when the app starts
void main() {
  runApp(const MaterialApp(home: QuizScreen()));
}
